# Optional Google Cloud Translation API key. Keep the real value in .env or your secret manager.
GOOGLE_TRANSLATE_API_KEY=
APP_PORT=8080
# Optional allowed origin if the translation proxy is accessed directly.
APP_ORIGIN=
